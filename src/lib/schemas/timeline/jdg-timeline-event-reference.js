// an event reference is one entity tagging another in an event
// the reference must provide this data so the original event can be looked up
const jdgTimelineEventReference = {
	sourceId: undefined,
	sourceName: undefined,
	eventId: undefined
};

export default jdgTimelineEventReference;
